<%= simple_form_for @conversation do |f| %>
  Who's joining the conversation today?
  <%= f.fields_for :people do |fp| %>
    <%= fp.input :name, label: false, placeholder: "Name" %>
  <% end %>
  <%= f.submit %>
<% end %>
